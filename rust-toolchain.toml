# Pin the exact Rust toolchain used for s7s development and releases so every
# contributor and CI run gets identical build, test, and Clippy results.
# Bump deliberately (and re-run scripts/check.sh) rather than tracking `stable`.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
